#include <stdio.h>

int main(){
	const char unsigned TILIN = 9;

	if(TILIN == 5)
		printf("bro momento");
	else if (TILIN == 7)
		printf("eso mamona");
	else
		printf("eso tilin");
	
    switch (TILIN)
	{
	case 1:
		break;
	case 2:
		break;
	default:
		break;
	}

	return 0;
}